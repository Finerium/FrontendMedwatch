# Backend API URL (server-side only, never NEXT_PUBLIC_).
# Local dev: http://127.0.0.1:8080
# Production: the Cloud Run service URL (configured in Vercel project settings).
BACKEND_API_URL=http://127.0.0.1:8080
